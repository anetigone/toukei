
# Toukei - 代码统计工具

Toukei 是一个用 Rust 编写的高性能代码统计工具，支持多种编程语言的代码行数统计和分析。它提供了同步和异步两种处理模式，并支持多种输出格式。

## 特性

- 🚀 高性能：支持多线程并行处理
- 📊 全面统计：统计代码行、注释行、空白行、函数和类数量
- 🌐 多语言支持：支持 40+ 种主流编程语言(maybe)
- 💾 多种输出格式：支持文本、JSON、CSV 格式输出
- ⚡ 异步处理：提供异步处理模式，提升大项目处理效率
- 🔧 灵活配置：支持自定义排除文件、工作线程数等

## 快速开始

### 安装

```bash
cargo install toukei
```

### 基本使用

```bash
# 统计当前目录
toukei

# 统计指定目录
toukei -p /path/to/project

# 异步模式处理（更快）
toukei --async

# 指定输出格式
toukei --output json
```

### 编程使用

#### 同步模式

```rust
use toukei::config::Config;
use toukei::fc::FileCounter;

let config = Config::new()
    .with_paths(vec!["./src".to_string()])
    .with_num_workers(4);

let counter = FileCounter::new(config);
let report = counter.process()?;
```

#### 异步模式

```rust
use toukei::config::Config;
use toukei::fc::AsyncFileCounter;

let config = Config::new()
    .with_paths(vec!["./src".to_string()]);

let counter = AsyncFileCounter::new(config);
let report = counter.process().await?;
```

## 配置选项

- `paths`: 要扫描的目录路径
- `types`: 要统计的语言类型
- `exclude_files`: 要排除的文件/目录
- `num_workers`: 工作线程数（异步模式）
- `output`: 输出格式（text/json/csv）
- `ignore_blanks`: 是否忽略空白行
- `ignore_comments`: 是否忽略注释行

## 支持的语言

- C/C++
- Rust
- Python
- JavaScript/TypeScript
- Java
- Go
- 以及更多...

## 输出示例

```
Language    Files     Lines      Code       Comments   Blanks     Functions
------------------------------------------------------------------------
Rust        15        2341       1892       234        215        42
Python      8         1234       987        123        124        23
------------------------------------------------------------------------
Total       23        3575       2879       357        339        65
```

## 性能对比

异步模式相比同步模式可以提供更好的性能，特别是在处理大型项目时：

- 同步模式：串行处理，适合小型项目
- 异步模式：并行处理，可自定义工作线程数，适合大型项目

## 贡献

欢迎提交 Issue 和 Pull Request！

## 许可证

MIT License